import React from "react";
import { Flex } from "@chakra-ui/core";

const CourseView = ({ course, isEditing }) => {

    return (
        <Flex>
            {/* editable pane */}
            {/* requirements pane */}
        </Flex>
    );
}

export default CourseView;